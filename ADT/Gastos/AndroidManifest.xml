<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dazaba.gastos"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.dazaba.gastos.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Registro" android:label="@string/app_name"></activity>
        <activity android:name="menuPrincipal" android:label="@string/app_name"></activity>
        <activity android:name="asociar_cta" android:label="@string/app_name"></activity>
        <activity android:name="incluir_gasto" android:label="@string/app_name"></activity>
        <activity android:name="gastos_x_cta" android:label="@string/app_name"></activity>
    </application>
	<uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>
